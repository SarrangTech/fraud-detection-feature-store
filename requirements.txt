# ── Streaming (Kafka producer / local consumer testing) ─────────────────────
kafka-python==3.0.9
python-dotenv==1.2.2

# ── Spark / Delta Lake (Structured Streaming bronze consumer, run on Databricks
#    or locally against a Spark session for dev/smoke-testing) ───────────────
pyspark==3.5.1
delta-spark==3.2.0

# ── dbt (silver feature engineering models against Databricks SQL) ──────────
dbt-core==1.8.8
dbt-databricks==1.8.5

# ── Databricks platform SDKs ─────────────────────────────────────────────────
databricks-sdk==0.29.0
databricks-feature-engineering==0.6.0

# ── ML training + tracking ───────────────────────────────────────────────────
mlflow==3.15.0
scikit-learn==1.9.0
imbalanced-learn==0.12.3
pandas==2.3.3
numpy==2.5.1

# ── Online feature store + real-time scoring service ─────────────────────────
redis==8.1.0
fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
pydantic-settings==2.14.2

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
fakeredis==2.37.0
